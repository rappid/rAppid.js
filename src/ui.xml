<?xml version="1.0"?>
<js:Application xmlns="http://www.w3.org/1999/xhtml"
                xmlns:js="js.core" xmlns:ui="js.ui" xmlns:conf="js.conf">

    <js:Imports>
        js.core.Base;
    </js:Imports>

    <js:Script>
        (function () {
            return {
                _defaults:{

                },
                /**
                 * Initializes the app
                 * this method is called right after the script tag is evaluated
                 */
                initialize:function () {
                    this.set('success', "he");
                    this.set('btnType', "success");
                    this.set('btnLabel',"wait");
                },
                onButtonClick: function(e){
                    console.log(e);
                    e.$.preventDefault();
                },
                /**
                 * This method should be called from outside to start the application
                 */
                start:function () {

                    var self = this;
                    setInterval(function () {
                        self.set("btnLabel"," what");
                        self.set("btnType","info");
                    },2000);

                    this.render(document.body);
                }
            }
        })();
    </js:Script>

    <div>
        <ui:Button tagName="a" iconClass="icon-refresh icon-white" disabled="false" label="{btnLabel}" type="{btnType}" onclick="onButtonClick"/>
        <!--<ui:MenuButton type="info" label="MenuButton">-->
            <!--<div>My DropdownMenu</div>-->
        <!--</ui:MenuButton>-->
        <!--<ui:SplitMenuButton/>-->
    </div>
</js:Application>